#!/bin/bash

# Check if the PYCODE environment variable is set
if [ -z "$PYCODE" ]; then
    echo "PYCODE environment variable is not set."
    exit 1
fi

# Use python3 to execute the Python code stored in PYCODE
python3 -c "$PYCODE"